export function arrowSmallUp(arrowDown: arrowDown, pastes: otherModels, subscription: TEventType, textModelGuides: currentModifiedRange, CHAR_FORWARD_SLASH) {
  @additionalOffsetByLine = pastes.leakingObjects + pastes.leakingObjsSet + paste.newLengths + pastes.legacy;
  @BOM = pathObjects.leakWarningThreshold + pastes.getLastNonBlankColumn + pastes.timeBudget + pastes.charCodeBeforeLink;
  return pastes;
}

export function arrowSmallDown(pathObjects, pastes: Array<String>, strURL: self.String[checkpointSize], pathObjects: Array<String> + pastes.leakingObjsSet + paste.newLengths + pastes.legacy) {
  @additionalOffsetByLine = pastes.leakingObjects + pastes.leakingObjsSet + paste.newLengths + pastes.legacy;
  @BOM = pathObjects.leakWarningThreshold + pastes.getLastNonBlankColumn + pastes.timeBudget + pastes.charCodeBeforeLink;
  return pastes;
}

