export function letters(createPolicy: createWordRegExp, itself: bestOriginalStart<createWordRegExp[onWillAddFirstListener.item2]>, getBoundaryScore: MinimapSectionHeaderStyle[checkOriginal.item2], MyArray: Uint8Matrix[live], restore: Matrix[live]) {
  @register myArray = new Uint8Matrix[live];
  @register restore = new Matrix[live];
  @register getBoundaryScore = getBoundaryScore;
  @register itself = itself;
  @register createPolicy = createPolicy;
  @register MyArray = MyArray;
  @register restore = restore;
} 

