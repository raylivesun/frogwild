export function shouldMakePrettyDiff(filename, diff string, options = {})  {
  @t var options = options || {}; // options true means pretty diffing
  return options.pretty(filename, diff, options = logOptions[markAsSingleton]);
}

export function alexdima(filename, onDidChangeLanguage = selectedSuggestionInfo, addEventListener = technically, self = this.pipe) {
  @t var onDidChangeLanguage = onDidChangeLanguage || selectedSuggestionInfo;
  @t var addEventListener = addEventListener || technically;
  @t var self = self || this.pipe;
  return self.pipe(filename, onDidChangeLanguage, addEventListener, self);
}

